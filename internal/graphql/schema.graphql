schema {
  query: Query,
  mutation: Mutation
}

type Query {
    placeHolder : Boolean
}

type Mutation {
    placeHolder : Boolean
}

type Subscription {
  placeHolder : Boolean
}

interface Node {
    id: Int!
}